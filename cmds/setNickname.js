module.exports.run = async (bot, message, args) => {
//     if(!message.member.hasPermission("ADMINISTRATOR")) return message.channel.send("You don't have permission!");
//     if(!args[0]) return message.channel.send("Please don't forget to add the link of image!");
//   let target = message.mentions.users.first() || message.author;
//   //console.log(message.content.slice(13));
//   console.log(target.id);
// await message.guild.member(target).setNickname(message.content.slice(13),"Why not");


}

module.exports.help = {
	name: "setnickname",
    help: "not ready yet!!",

}